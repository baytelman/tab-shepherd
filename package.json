{
  "name": "tab-shepherd",
  "version": "1.2.1",
  "description": "Automatically route tabs to named windows based on URL patterns",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
